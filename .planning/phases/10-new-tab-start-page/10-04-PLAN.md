---
phase: 10-new-tab-start-page
plan: 04
type: execute
wave: 3
depends_on:
  - "10-02"
  - "10-03"
files_modified: []
autonomous: false
requirements:
  - START-01
  - START-02
  - START-03
  - START-04
  - START-05

must_haves:
  truths:
    - "New Tab shows the Prometheus start page — PM mark, wordmark, sub-line, omnibox, favorites grid, suggestion strip"
    - "Clicking Ask FSB with text opens the side panel in FSB Agent mode with the prompt pre-filled — NOT auto-submitted"
    - "Favorites grid shows real bookmark data; clicking a tile navigates the tab"
    - "Suggestion card click seeds the correct prompt in the FSB Agent composer"
    - "Warm dark theme tokens render correctly (#141110 background, #ff6b35 accent)"
    - "Warm light theme tokens render correctly (#fffdfb background) after toggling the theme"
    - "No network font or icon requests in browser DevTools (all assets load from qrc:)"
  artifacts:
    - path: "falkon/src/lib/data/html/start.html"
      provides: "Approved start page HTML"
      contains: "qz_prometheus_start"
    - path: "falkon/src/lib/agent/prometheusstartbridge.cpp"
      provides: "Bridge implementation"
      contains: "openAgentWithPrompt"
  key_links:
    - from: "omnibox Ask FSB button"
      to: "FSB Agent side panel composer"
      via: "bridge.openAgentWithPrompt → seedPromptAndOpen"
      pattern: "qz_prometheus_start"
---

<objective>
Visual and functional verification checkpoint for the Prometheus New Tab start page. Confirm all five START requirements are met by running the browser and manually exercising every interaction surface.

Purpose: Human approval is required because the design system rendering, theme correctness, prompt handoff behavior (non-auto-submit), and offline asset loading cannot be fully verified by automated checks alone.
Output: Human sign-off confirming visual and functional correctness across both themes. No new files created.
</objective>

<execution_context>
@/Users/lakshman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-new-tab-start-page/10-CONTEXT.md
@.planning/phases/10-new-tab-start-page/10-UI-SPEC.md
@.planning/phases/10-new-tab-start-page/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and start the browser</name>
  <files></files>
  <read_first>
    No files to read. This task runs the build and launches the browser.
  </read_first>
  <action>
Run a clean build of the Falkon library and application to incorporate all Phase 10 changes:

  cd falkon && cmake --build build --target all -j4 2>&1 | tail -20

Confirm the build exits with status 0. If there are compile errors, fix them before proceeding to the checkpoint.

Then launch the browser:

  open falkon/build/bin/Prometheus.app   # macOS
  # OR: ./falkon/build/bin/prometheus    # Linux

The browser should open with at least one window. Proceed to the checkpoint task for human verification.
  </action>
  <verify>
    <automated>
      cmake --build /Users/lakshman/conductor/workspaces/prometheus/puebla/falkon/build --target all -j4 2>&1 | tail -5 | grep -v "^$" | head -3
    </automated>
  </verify>
  <done>Build exits with status 0 and browser launches without crash.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional sign-off for Phase 10 start page</name>
  <what-built>
    The Prometheus New Tab start page — replacing the legacy Falkon speeddial — with:
    - PM mark hero (CSS replica, 64×64, orange gradient, P-Thin / M-Black)
    - "Prometheus." wordmark (Poppins 600 34px, trailing "." dimmed)
    - Sub-line: "Search the web, or ask FSB to do it for you."
    - Pill omnibox with magnifier icon, placeholder text, and orange "Ask FSB" button
    - Favorites grid populated from real bookmarks (first 8 URL bookmarks, BFS)
    - "Suggested automations" strip with 3 cards (Triage / Groceries / Meeting)
    - QWebChannel bridge (qz_prometheus_start) for all native handoffs
    - Both warm dark (#141110) and warm light (#fffdfb) themes
    - All fonts/icons from qrc: resources (no network)
  </what-built>
  <how-to-verify>
    Complete EACH check and note any failures:

    **START-01: Hero surface**
    1. Open a new tab (Cmd+T / Ctrl+T). Confirm the URL bar shows `falkon:start`.
    2. Confirm the PM mark renders: 64×64 rounded square, orange-to-coral gradient, white "P" (thin) and "M" (bold) letters.
    3. Confirm "Prometheus." heading with trailing dot in a dimmer color.
    4. Confirm sub-line: "Search the web, or ask FSB to do it for you."
    5. Confirm a subtle orange radial glow at the top center of the page in dark theme.

    **START-02: Omnibox and Ask FSB handoff**
    6. Confirm the omnibox is focused on page load (cursor visible in the input without clicking).
    7. Confirm placeholder text: Search or enter address — try "book me a flight to Tokyo"
    8. Type "https://github.com" and press Enter — confirm the tab navigates to github.com (not the agent panel).
    9. Open a new tab again. Type "summarize this morning's emails" and press Enter — confirm the Prometheus side panel opens in FSB Agent mode with the prompt pre-filled in the composer. Confirm the task does NOT auto-run.
    10. Open a new tab. Type text in the omnibox and click the orange "Ask FSB" button — confirm same side panel behavior as step 9.
    11. Open a new tab. Press Escape in the omnibox — confirm the input clears.

    **START-03: Favorites grid**
    12. Confirm the favorites grid shows tiles for real bookmarks (add a bookmark via Cmd+D if none exist, then reload the new tab).
    13. Confirm each tile: colored 48×48 icon square with 2-letter uppercase mono abbreviation, name below.
    14. Click a favorites tile — confirm the tab navigates to that bookmark URL.
    15. If you have no bookmarks: confirm the empty-state message appears ("No favorites yet / Open a page and bookmark it").

    **START-04: Suggested automations**
    16. Confirm three suggestion cards: "Triage my inbox", "Reorder groceries", "Find a meeting slot".
    17. Confirm each card shows the correct icon (inbox envelope, shopping cart, calendar with checkmark), title, and sub-text.
    18. Click "Triage my inbox" — confirm the side panel opens in FSB Agent mode with the correct prompt pre-filled ("Triage my inbox — archive newsletters and draft replies to anything from my team."). Confirm NOT auto-submitted.
    19. Check keyboard accessibility: Tab through the page — confirm omnibox, Ask FSB button, favorite tiles, and suggestion cards are all reachable and activatable.

    **START-05: Design system**
    20. In dark theme: confirm page background is near-black (#141110 range), accent orange (#ff6b35) on Ask FSB button and card icons, primary text is warm white (#f6efe9 range).
    21. Toggle to light theme (via the theme toggle button in the browser toolbar). Reload the new tab. Confirm page background is near-white (#fffdfb range), accent orange unchanged, text is near-black (#1f1a17 range).
    22. Open browser DevTools (F12 → Network tab, filter by "Font"). Load a new tab. Confirm NO external font requests (all fonts from qrc: which appear as local resources, not network).
    23. In DevTools Network tab, confirm NO requests to fontawesome.com or any CDN for icons.
    24. Confirm Poppins renders correctly for the wordmark and PM mark letters (visually distinct from system UI fallback).
    25. Confirm Space Mono renders for the "FAVORITES N" section label and the "agent" badge.

    **Responsive check**
    26. Narrow the browser window (drag side panel open simultaneously). At ~720px content width, confirm the three suggestion cards collapse to a single column. At wider widths, confirm 3-column layout.
  </how-to-verify>
  <verify>
    <human-check>All 26 checks described above must pass. Note any visual discrepancies with the exact values specified in 10-UI-SPEC.md (e.g., wrong color values, wrong font weights, wrong spacing).</human-check>
  </verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific failures with the check number (e.g., "check 9 failed: prompt auto-submitted") for a targeted fix pass.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human reviewer → browser | Human interacts with the browser to verify behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-V1 | Information Disclosure | DevTools Network panel inspection during verification | accept | Intentional — verifying no external asset requests is part of START-05 |
| T-10-V2 | Tampering | Human accidentally auto-submitting agent during check 9 | accept | Check 9 requires human to inspect the composer state before pressing Run — no automated risk |
</threat_model>

<verification>
Verification is complete when the human reviewer types "approved" in response to the checkpoint.

Automated pre-check (run before notifying the user):
  cmake --build falkon/build --target all -j4 2>&1 | grep "error:" | wc -l | grep "^0$" || echo "BUILD ERRORS — fix before verification"
</verification>

<success_criteria>
Human reviewer approves all 26 checks:
- START-01: Hero (PM mark, wordmark, sub-line) visible and correctly styled.
- START-02: Omnibox focused on load; URL input navigates; natural-language input seeds agent panel (non-auto-submit); Ask FSB button works; Escape clears.
- START-03: Favorites grid populated from real bookmarks; tile click navigates; empty state shows when no bookmarks.
- START-04: Three suggestion cards present with correct icons, copy, and prompts; click seeds agent panel without auto-submit.
- START-05: Dark and light themes both render correctly; no network font/icon requests confirmed in DevTools; Poppins and Space Mono visually correct.
</success_criteria>

<output>
Create `.planning/phases/10-new-tab-start-page/10-04-SUMMARY.md` when done.
</output>
