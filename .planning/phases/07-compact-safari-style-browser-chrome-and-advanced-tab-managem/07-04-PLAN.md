---
phase: 07-compact-safari-style-browser-chrome-and-advanced-tab-managem
phase_number: "07"
plan: "04"
plan_id: 07-04
type: execute
wave: 3
depends_on: [07-01, 07-02, 07-03]
files_modified:
  - falkon/src/lib/navigation/navigationbar.h
  - falkon/src/lib/navigation/navigationbar.cpp
  - falkon/src/lib/tabwidget/tabwidget.h
  - falkon/src/lib/tabwidget/tabwidget.cpp
autonomous: true
requirements: [UX-01, UX-02, UX-05]
requirements_addressed: [UX-01, UX-02, UX-05]
user_setup: []
must_haves:
  truths:
    - "Compact mode uses one primary chrome row with inactive tabs and active address/search in the toolbar."
    - "Separate mode preserves the conventional toolbar plus tab bar behavior."
    - "Open-location shortcuts still focus the current tab's existing LocationBar."
  artifacts:
    - path: "falkon/src/lib/navigation/navigationbar.cpp"
      provides: "Compact toolbar composition"
      contains: "CompactTabStrip"
    - path: "falkon/src/lib/tabwidget/tabwidget.cpp"
      provides: "Separate tab bar visibility switching"
      contains: "tabLayout"
  key_links:
    - from: "NavigationBar"
      to: "TabWidget::locationBars"
      via: "active compact address/search field"
      pattern: "locationBars"
    - from: "TabWidget::loadSettings"
      to: "qzSettings->tabLayout"
      via: "show/hide separate tab bar"
      pattern: "tabLayout"
---

<objective>
Wire compact mode into the main browser chrome while preserving separate layout.

Purpose: Users need the Safari-inspired compact row, but the existing Falkon layout must remain available and compatible.
Output: Runtime compact/separate switching in `NavigationBar` and `TabWidget`.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-compact-safari-style-browser-chrome-and-advanced-tab-managem/07-UI-SPEC.md
@.planning/phases/07-compact-safari-style-browser-chrome-and-advanced-tab-managem/07-RESEARCH.md
@.planning/phases/07-compact-safari-style-browser-chrome-and-advanced-tab-managem/07-PATTERNS.md
@.planning/phases/07-compact-safari-style-browser-chrome-and-advanced-tab-managem/07-01-SUMMARY.md
@.planning/phases/07-compact-safari-style-browser-chrome-and-advanced-tab-managem/07-03-SUMMARY.md

<interfaces>
Existing active address contract:
```cpp
m_navigationSplitter->addWidget(m_window->tabWidget()->locationBars());
LocationBar* BrowserWindow::locationBar() const;
void BrowserWindow::openLocation() { locationBar()->setFocus(); locationBar()->selectAll(); }
```
Do not duplicate URL/search parsing or completion. Compact mode must reuse `TabWidget::locationBars()`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compact row composition to NavigationBar</name>
  <read_first>
    - @falkon/src/lib/navigation/navigationbar.h
    - @falkon/src/lib/navigation/navigationbar.cpp
    - @falkon/src/lib/tabwidget/compacttabstrip.h
    - @.planning/phases/07-compact-safari-style-browser-chrome-and-advanced-tab-managem/07-UI-SPEC.md
  </read_first>
  <files>falkon/src/lib/navigation/navigationbar.h, falkon/src/lib/navigation/navigationbar.cpp</files>
  <action>
    Instantiate `CompactTabStrip` in `NavigationBar` and register it as a toolbar widget with an ID such as `compact-tabs`. In `loadSettings()` read the canonical layout from Plan 07-01. For Compact, compose one row containing navigation buttons, reload/stop, add-tab, compact strip, the existing `m_navigationSplitter` with `TabWidget::locationBars()` as the active address/search field, Prometheus agent/sidebar entry, and tools menu. Hide the standalone `WebSearchBar` in compact mode because the active tab/address field is the unified search surface. Enforce the UI-spec size constraints: 32px content, 40px max row, active address min 180/preferred 320/max 520 where Qt policies allow it. For Separate, preserve the current default layout IDs and search bar behavior.
  </action>
  <verify>
    <automated>git diff --check && rg "CompactTabStrip|compact-tabs|tabLayout|ShowSearchBar|locationbar" falkon/src/lib/navigation/navigationbar.h falkon/src/lib/navigation/navigationbar.cpp</automated>
  </verify>
  <acceptance_criteria>
    - Compact mode puts compact tabs and the existing location bar stack in the same toolbar row.
    - Separate mode follows the existing `NavigationBar::loadSettings()` layout path.
    - Compact mode does not create a second URL/search parser or input.
  </acceptance_criteria>
  <done>NavigationBar can render compact and separate layouts from the persisted setting.</done>
</task>

<task type="auto">
  <name>Task 2: Switch traditional tab bar visibility by layout</name>
  <read_first>
    - @falkon/src/lib/tabwidget/tabwidget.h
    - @falkon/src/lib/tabwidget/tabwidget.cpp
    - @falkon/src/lib/tabwidget/tabbar.cpp
    - @falkon/src/lib/app/browserwindow.cpp
  </read_first>
  <files>falkon/src/lib/tabwidget/tabwidget.h, falkon/src/lib/tabwidget/tabwidget.cpp</files>
  <action>
    Update `TabWidget::loadSettings()` so Compact hides the conventional `m_tabBar` row and Separate shows it exactly as before. Keep `m_locationBars` and current-tab synchronization unchanged. Expose a small read method if needed, such as `bool isCompactLayout() const`, for later tab-management UI. Verify `Ctrl+L`, `Alt+D`, and File > Open Location still route to `BrowserWindow::openLocation()` and focus the current tab's existing `LocationBar`; do not alter URL/search completion behavior.
  </action>
  <verify>
    <automated>git diff --check && rg "isCompactLayout|tabLayout|setVisible|m_tabBar->loadSettings|locationBars" falkon/src/lib/tabwidget/tabwidget.h falkon/src/lib/tabwidget/tabwidget.cpp falkon/src/lib/app/browserwindow.cpp</automated>
  </verify>
  <acceptance_criteria>
    - Compact hides only the conventional tab bar, not tab contents or location bars.
    - Separate keeps existing tab bar behavior, add-tab behavior, overflow behavior, and closed-tabs button behavior.
    - Existing open-location shortcuts still focus and select the current LocationBar.
  </acceptance_criteria>
  <done>Runtime layout switching preserves conventional tab behavior while enabling compact chrome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings -> top-level chrome | Profile setting selects layout for every browser window. |
| tab/page data -> toolbar | Page titles and tab states render in primary chrome. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Spoofing | Compact active address | mitigate | Reuse existing `LocationBar` and site/security icon behavior so secure-origin cues are not recreated or confused with automation badges. |
| T-07-04-02 | Denial of Service | `NavigationBar::reloadLayout` | mitigate | Keep fixed/min/max policies and hide/reparent widgets using the existing reload discipline to avoid overlapping widgets during setting changes. |
| T-07-04-03 | Tampering | Compact tab actions | mitigate | Compact strip actions route to existing `TabWidget` operations; layout code must not bypass tab ownership checks added elsewhere. |
</threat_model>

<verification>
Run:
```bash
git diff --check
rg "CompactTabStrip|compact-tabs|tabLayout|locationBars|isCompactLayout" falkon/src/lib/navigation falkon/src/lib/tabwidget
if test -d falkon/build; then cmake --build falkon/build --target help; fi
```
</verification>

<success_criteria>
- Compact mode moves inactive tab display into the toolbar row and makes the active tab use the current LocationBar.
- Separate mode remains available and conventional.
- Agent-aware state badges from Plan 07-02 have a compact chrome rendering path through Plan 07-03.
</success_criteria>

<output>
After completion, create `.planning/phases/07-compact-safari-style-browser-chrome-and-advanced-tab-managem/07-04-SUMMARY.md`.
</output>
