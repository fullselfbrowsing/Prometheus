---
phase: 07-compact-safari-style-browser-chrome-and-advanced-tab-managem
phase_number: "07"
plan: "09"
plan_id: 07-09
type: execute
wave: 7
depends_on: [07-01, 07-02, 07-03, 07-04, 07-05, 07-06, 07-07, 07-08]
files_modified:
  - tools/fsb-baseline/smoke-compact-tabs.sh
  - tools/fsb-baseline/release-validate.sh
  - .planning/phases/07-compact-safari-style-browser-chrome-and-advanced-tab-managem/07-VERIFICATION.md
autonomous: false
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05, UX-06]
requirements_addressed: [UX-01, UX-02, UX-03, UX-04, UX-05, UX-06]
user_setup: []
must_haves:
  truths:
    - "Static/build validation covers compact settings, chrome classes, tab state, group roles, and native reads."
    - "Manual visual verification covers compact/separate layouts across narrow, standard, and wide desktop widths."
    - "Final verification records pass/fail state for UI spec scenarios and command checks."
  artifacts:
    - path: "tools/fsb-baseline/smoke-compact-tabs.sh"
      provides: "Automated compact tab/state smoke checks"
      contains: "list_tabs"
    - path: "tools/fsb-baseline/release-validate.sh"
      provides: "Release validation integration"
      contains: "smoke-compact-tabs"
    - path: ".planning/phases/07-compact-safari-style-browser-chrome-and-advanced-tab-managem/07-VERIFICATION.md"
      provides: "Phase verification record"
      contains: "800px"
  key_links:
    - from: "smoke-compact-tabs.sh"
      to: "AgentCommandRouter list_tabs"
      via: "native command smoke"
      pattern: "list_tabs"
    - from: "07-VERIFICATION.md"
      to: "07-UI-SPEC.md"
      via: "manual visual checklist"
      pattern: "Compact|Separate|Search Tabs|Tab Overview"
---

<objective>
Add final automated smoke coverage and human visual verification for compact chrome and advanced tab management.

Purpose: Phase 7 is UI-heavy, so static/build checks must be paired with an explicit rendered-state checklist before completion.
Output: Smoke script, release validation hook, verification artifact, and a blocking human visual checkpoint.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-compact-safari-style-browser-chrome-and-advanced-tab-managem/07-UI-SPEC.md
@.planning/phases/07-compact-safari-style-browser-chrome-and-advanced-tab-managem/07-VALIDATION.md
@.planning/phases/07-compact-safari-style-browser-chrome-and-advanced-tab-managem/07-RESEARCH.md
@.planning/phases/07-compact-safari-style-browser-chrome-and-advanced-tab-managem/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compact tab smoke script and release validation hook</name>
  <read_first>
    - @tools/fsb-baseline/smoke-agent-control.sh
    - @tools/fsb-baseline/release-validate.sh
    - @.planning/phases/07-compact-safari-style-browser-chrome-and-advanced-tab-managem/07-VALIDATION.md
  </read_first>
  <files>tools/fsb-baseline/smoke-compact-tabs.sh, tools/fsb-baseline/release-validate.sh</files>
  <action>
    Add `tools/fsb-baseline/smoke-compact-tabs.sh` following the existing smoke script style. It should accept `--binary` and `--port`, launch Prometheus with `PROMETHEUS_AGENT_PORT`, exercise native commands to create/list tabs, create an owned tab, create/end supervision, and assert `list_tabs` returns layout-relevant state fields: owner, activeAutomation, supervisionActive, health, groupId, groupName, groupCollapsed. Include static `rg` checks for settings keys and compact/search/group classes. Add the smoke script to `release-validate.sh` after agent/MCP smoke when a binary is available.
  </action>
  <verify>
    <automated>git diff --check && test -x tools/fsb-baseline/smoke-compact-tabs.sh && rg "smoke-compact-tabs|list_tabs|activeAutomation|supervisionActive|groupId|tabLayout|TabSearchPopup|CompactTabStrip" tools/fsb-baseline/smoke-compact-tabs.sh tools/fsb-baseline/release-validate.sh</automated>
  </verify>
  <acceptance_criteria>
    - Smoke script is executable and follows existing argument style.
    - Script checks both source/static fields and live `list_tabs` state fields when a binary can run.
    - Release validation invokes the compact tab smoke.
  </acceptance_criteria>
  <done>Automated Phase 7 smoke validation is available and release-gated.</done>
</task>

<task type="auto">
  <name>Task 2: Create final verification record</name>
  <read_first>
    - @.planning/phases/07-compact-safari-style-browser-chrome-and-advanced-tab-managem/07-UI-SPEC.md
    - @.planning/phases/07-compact-safari-style-browser-chrome-and-advanced-tab-managem/07-VALIDATION.md
  </read_first>
  <files>.planning/phases/07-compact-safari-style-browser-chrome-and-advanced-tab-managem/07-VERIFICATION.md</files>
  <action>
    Create `07-VERIFICATION.md` with sections for automated checks, build/static results, smoke results, and manual visual checklist. The visual checklist must include compact/separate layout, title-and-icon/favicon-only display, active automation, supervised tab, warning tab, private window, grouped tabs, overflow tabs, tab overview, search empty/error states, and widths 800px, 1280px, and 1728px+. Record exact commands run and whether GUI/manual checks were passed, failed, or not run with reason.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-compact-safari-style-browser-chrome-and-advanced-tab-managem/07-VERIFICATION.md && rg "800px|1280px|1728px|Compact|Separate|Title and Icon|Favicon Only|Tab Overview|Search Tabs|Supervised session active|smoke-compact-tabs" .planning/phases/07-compact-safari-style-browser-chrome-and-advanced-tab-managem/07-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Verification file exists and maps checks to UX-01 through UX-06.
    - It records commands and results, not only intended checks.
    - It includes every manual visual state from `07-VALIDATION.md`.
  </acceptance_criteria>
  <done>Phase verification evidence is captured in the phase directory.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual verification checkpoint</name>
  <read_first>
    - @.planning/phases/07-compact-safari-style-browser-chrome-and-advanced-tab-managem/07-VERIFICATION.md
    - @.planning/phases/07-compact-safari-style-browser-chrome-and-advanced-tab-managem/07-UI-SPEC.md
  </read_first>
  <files>.planning/phases/07-compact-safari-style-browser-chrome-and-advanced-tab-managem/07-VERIFICATION.md</files>
  <action>
    After automated checks pass, ask the user to verify the rendered desktop browser. Provide exact steps: launch the built Prometheus binary, switch Tab Layout between Compact and Separate, switch Tab Display between Title and Icon and Favicon Only, resize to 800px/1280px/1728px+, create enough tabs for overflow, create a group, open Tab Overview and Search Tabs, filter to empty state, and inspect owner/automation/supervision/health states when available. Append the user's approval or issues to `07-VERIFICATION.md`.
  </action>
  <acceptance_criteria>
    - User confirms compact chrome has no text/icon overlap at 800px, 1280px, and 1728px+.
    - User confirms separate layout remains conventional.
    - User confirms overview/search/group/action states match the approved UI spec.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/07-compact-safari-style-browser-chrome-and-advanced-tab-managem/07-VERIFICATION.md && rg "Human visual verification|approved|issues|800px|1280px|1728px" .planning/phases/07-compact-safari-style-browser-chrome-and-advanced-tab-managem/07-VERIFICATION.md</automated>
  </verify>
  <what-built>Compact chrome, tab overview/search, tab groups, advanced tab actions, and agent-aware tab state are implemented and smoke-checked.</what-built>
  <how-to-verify>
    1. Launch the built Prometheus app.
    2. In Preferences > Tabs, set `Tab Layout` to `Compact`, then verify the active tab uses the address/search field in the toolbar.
    3. Resize to 800px, 1280px, and 1728px+ and verify no text/icon overlap; Search Tabs remains reachable.
    4. Toggle `Tab Display` between `Title and Icon` and `Favicon Only`; row height must not change.
    5. Create grouped, pinned, loading/restored, supervised/agent-owned, and warning-state tabs where available.
    6. Open `Tab Overview` and `Search Tabs`, test filtering, empty state, row actions, and keyboard navigation.
  </how-to-verify>
  <resume-signal>Type `approved` or describe the visual/functionality issues.</resume-signal>
  <done>Human visual verification is recorded in `07-VERIFICATION.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validation scripts -> local browser | Smoke scripts launch and drive the local binary through the native command port. |
| human verification -> planning artifact | Manual observations are recorded as phase evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09-01 | Spoofing | Smoke command port | mitigate | Use caller-provided local port and existing loopback-only Prometheus agent server behavior. |
| T-07-09-02 | Tampering | Verification record | mitigate | Record exact commands and observed results so later verification can audit what passed. |
| T-07-09-03 | Repudiation | Human visual approval | mitigate | Append the approval/issues and date to `07-VERIFICATION.md`. |
| T-07-09-04 | Denial of Service | Smoke script | mitigate | Use bounded waits and clean up launched browser process on exit. |
</threat_model>

<verification>
Run:
```bash
git diff --check
tools/fsb-baseline/smoke-compact-tabs.sh --help
rg "smoke-compact-tabs|800px|1280px|1728px|Tab Overview|Search Tabs" tools/fsb-baseline .planning/phases/07-compact-safari-style-browser-chrome-and-advanced-tab-managem/07-VERIFICATION.md
if test -d falkon/build; then cmake --build falkon/build --target help; fi
```
</verification>

<success_criteria>
- `UX-01` through `UX-05` are covered by automated/static smoke checks.
- `UX-06` is covered by recorded visual verification across required desktop widths.
- The final verification artifact exists and is suitable for `$gsd-verify-work`.
</success_criteria>

<output>
After completion, create `.planning/phases/07-compact-safari-style-browser-chrome-and-advanced-tab-managem/07-09-SUMMARY.md`.
</output>
