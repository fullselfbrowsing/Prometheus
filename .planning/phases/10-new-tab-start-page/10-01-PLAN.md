---
phase: 10-new-tab-start-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - falkon/src/lib/data/icons/fa/inbox.svg
  - falkon/src/lib/data/icons/fa/cart-shopping.svg
  - falkon/src/lib/data/icons/fa/calendar-check.svg
  - falkon/src/lib/data/icons/icons.qrc
  - falkon/src/lib/tabwidget/tabwidget.cpp
autonomous: true
requirements:
  - START-01
  - START-04

must_haves:
  truths:
    - "New Tab opens `falkon:start` by default for fresh profiles"
    - "The three suggestion-card FA SVGs exist as Qt resources and are accessible via `qrc:icons/fa/inbox.svg`, `qrc:icons/fa/cart-shopping.svg`, `qrc:icons/fa/calendar-check.svg`"
  artifacts:
    - path: "falkon/src/lib/data/icons/fa/inbox.svg"
      provides: "Font Awesome Free 6.x inbox solid glyph"
      contains: "<svg"
    - path: "falkon/src/lib/data/icons/fa/cart-shopping.svg"
      provides: "Font Awesome Free 6.x cart-shopping solid glyph"
      contains: "<svg"
    - path: "falkon/src/lib/data/icons/fa/calendar-check.svg"
      provides: "Font Awesome Free 6.x calendar-check solid glyph"
      contains: "<svg"
    - path: "falkon/src/lib/data/icons.qrc"
      provides: "Qt resource manifest including the three new SVGs"
      contains: "icons/fa/inbox.svg"
  key_links:
    - from: "falkon/src/lib/data/html/start.html"
      to: "qrc:icons/fa/inbox.svg"
      via: "<img src='qrc:icons/fa/inbox.svg'>"
      pattern: "qrc:icons/fa/inbox\\.svg"
    - from: "falkon/src/lib/tabwidget/tabwidget.cpp:176"
      to: "falkon:start"
      via: "settings.value(QSL(\"newTabUrl\"), QSL(\"falkon:start\"))"
      pattern: "falkon:start"
---

<objective>
Bundle the three Font Awesome Free SVG glyphs required by the suggestion cards (inbox, cart-shopping, calendar-check) and repoint the new-tab default URL from `falkon:speeddial` to `falkon:start`.

Purpose: These are blocking prerequisites for the start page HTML (Plan 02). The three SVGs must be in `icons.qrc` before the HTML can reference them via `qrc:` URLs. The TabWidget default change makes new tabs open the Prometheus start page.
Output: Three SVG files in `falkon/src/lib/data/icons/fa/`, three new entries in `icons.qrc`, one line changed in `tabwidget.cpp`.
</objective>

<execution_context>
@/Users/lakshman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-new-tab-start-page/10-CONTEXT.md
@.planning/phases/10-new-tab-start-page/10-UI-SPEC.md
@.planning/phases/10-new-tab-start-page/10-PATTERNS.md

<interfaces>
<!-- From falkon/src/lib/data/icons.qrc lines 64-89 (FA subset block): -->
<!-- Entry pattern:
     <file>icons/fa/bolt.svg</file>
     New entries go after the existing FA subset block (before </qresource>).
     The <!-- Font Awesome Free 6.x subset — CC BY 4.0 — offline only --> comment
     appears at line 64 above the existing entries. -->

<!-- From falkon/src/lib/tabwidget/tabwidget.cpp line 176: -->
<!-- m_urlOnNewTab = settings.value(QSL("newTabUrl"), QSL("falkon:speeddial")).toUrl(); -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download and register the three missing FA SVG glyphs</name>
  <files>
    falkon/src/lib/data/icons/fa/inbox.svg
    falkon/src/lib/data/icons/fa/cart-shopping.svg
    falkon/src/lib/data/icons/fa/calendar-check.svg
    falkon/src/lib/data/icons.qrc
  </files>
  <read_first>
    Read falkon/src/lib/data/icons.qrc lines 60-100 to see the existing FA subset block and the comment/entry pattern.
    Read falkon/src/lib/data/icons/fa/bolt.svg to confirm the expected SVG structure (solid style, single-color path, viewBox attribute).
  </read_first>
  <action>
Download the three Font Awesome Free 6.x solid-style SVGs and register them in icons.qrc.

Download commands (each fetches the canonical solid SVG from the FA 6.x GitHub tree):

  curl -fsSL "https://raw.githubusercontent.com/FortAwesome/Font-Awesome/6.x/svgs/solid/inbox.svg" \
       -o falkon/src/lib/data/icons/fa/inbox.svg

  curl -fsSL "https://raw.githubusercontent.com/FortAwesome/Font-Awesome/6.x/svgs/solid/cart-shopping.svg" \
       -o falkon/src/lib/data/icons/fa/cart-shopping.svg

  curl -fsSL "https://raw.githubusercontent.com/FortAwesome/Font-Awesome/6.x/svgs/solid/calendar-check.svg" \
       -o falkon/src/lib/data/icons/fa/calendar-check.svg

Verify each SVG is non-empty and contains a valid `<svg` opening tag before proceeding.

In falkon/src/lib/data/icons.qrc, add the three new entries inside the existing FA subset block, immediately after the last `<file>icons/fa/...` entry. Follow the exact same `<file>icons/fa/NAME.svg</file>` format used by existing entries. Do NOT add new comments; simply append three lines:

  <file>icons/fa/inbox.svg</file>
  <file>icons/fa/cart-shopping.svg</file>
  <file>icons/fa/calendar-check.svg</file>

The CC BY 4.0 attribution comment already covers the whole block (it appears at the start of the FA subset section). No additional attribution comment is needed per entry.
  </action>
  <verify>
    <automated>
      test -s falkon/src/lib/data/icons/fa/inbox.svg &&
      test -s falkon/src/lib/data/icons/fa/cart-shopping.svg &&
      test -s falkon/src/lib/data/icons/fa/calendar-check.svg &&
      grep -c "fa/inbox.svg" falkon/src/lib/data/icons.qrc | grep -q "^1$" &&
      grep -c "fa/cart-shopping.svg" falkon/src/lib/data/icons.qrc | grep -q "^1$" &&
      grep -c "fa/calendar-check.svg" falkon/src/lib/data/icons.qrc | grep -q "^1$" &&
      grep "<svg" falkon/src/lib/data/icons/fa/inbox.svg | grep -q "svg" &&
      echo "PASS"
    </automated>
  </verify>
  <done>Three SVG files exist and are non-empty; each has exactly one entry in icons.qrc within the FA subset block.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint TabWidget new-tab default to `falkon:start`</name>
  <files>
    falkon/src/lib/tabwidget/tabwidget.cpp
  </files>
  <read_first>
    Read falkon/src/lib/tabwidget/tabwidget.cpp lines 170-185 to see the m_urlOnNewTab assignment and surrounding loadSettings context.
  </read_first>
  <action>
In falkon/src/lib/tabwidget/tabwidget.cpp at line 176, change the default value in the settings.value call from `QSL("falkon:speeddial")` to `QSL("falkon:start")`:

Before:
  m_urlOnNewTab = settings.value(QSL("newTabUrl"), QSL("falkon:speeddial")).toUrl();

After:
  m_urlOnNewTab = settings.value(QSL("newTabUrl"), QSL("falkon:start")).toUrl();

This is a single character-level substitution. Do not alter surrounding code. The settings key `newTabUrl` is unchanged; users who explicitly saved a custom newTabUrl value retain their setting. Only fresh profiles and profiles where the key was never explicitly written switch to the new default.

No other files require changes for this task.
  </action>
  <verify>
    <automated>
      grep -c "falkon:start" falkon/src/lib/tabwidget/tabwidget.cpp | grep -q "^1$" &&
      grep "newTabUrl" falkon/src/lib/tabwidget/tabwidget.cpp | grep -v "speeddial" | grep -q "falkon:start" &&
      echo "PASS"
    </automated>
  </verify>
  <done>`tabwidget.cpp` line 176 default is `"falkon:start"`, not `"falkon:speeddial"`; no other lines in that file contain `falkon:start`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curl download → local SVG file | SVGs fetched from GitHub CDN during build/setup; not user-facing at runtime |
| Qt resource compiler → binary | icons.qrc entries compiled into the binary at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-SC | Tampering | curl download of FA SVGs from GitHub | mitigate | Verify SVG files are non-empty and begin with `<svg` before committing; FA Free is CC BY 4.0 and the GitHub source is canonical |
| T-10-01 | Tampering | icons.qrc entry for new SVGs | accept | Qt resource compiler is build-time; attacker would need write access to source tree; outside ASVS L1 scope |

</threat_model>

<verification>
After completing both tasks, verify the overall state:

1. `ls -la falkon/src/lib/data/icons/fa/inbox.svg falkon/src/lib/data/icons/fa/cart-shopping.svg falkon/src/lib/data/icons/fa/calendar-check.svg` — all three exist and are non-zero size.
2. `grep -E "inbox|cart-shopping|calendar-check" falkon/src/lib/data/icons.qrc` — shows three matches.
3. `grep "newTabUrl" falkon/src/lib/tabwidget/tabwidget.cpp` — shows `"falkon:start"` as default.
4. Build check (optional but recommended): `cd falkon && cmake --build build --target FalkonPrivate -j4 2>&1 | tail -5` — no new compile errors.
</verification>

<success_criteria>
- Three FA SVGs (`inbox.svg`, `cart-shopping.svg`, `calendar-check.svg`) downloaded from Font Awesome Free 6.x (CC BY 4.0), placed in `falkon/src/lib/data/icons/fa/`, and registered in `icons.qrc`.
- `tabwidget.cpp` line 176 default changed to `"falkon:start"`.
- All three SVGs accessible via `qrc:icons/fa/inbox.svg`, `qrc:icons/fa/cart-shopping.svg`, `qrc:icons/fa/calendar-check.svg` in Qt resource builds.
</success_criteria>

<output>
Create `.planning/phases/10-new-tab-start-page/10-01-SUMMARY.md` when done.
</output>
